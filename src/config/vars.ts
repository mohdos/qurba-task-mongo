

export const vars = { // will hold any variables that may be changed during operation
    nearbyRestaurantsDistance: 1, // distance in KM to search for nearby restaurants
    ELASTICSEARCH_INDEX_ENDPOINT: '/restaurant-search' // endpoint used to create index for restaurant
};

